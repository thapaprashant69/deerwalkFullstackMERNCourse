let multipicant = 8;

for(let multiplier=1;multiplier<=10;multiplier++){
    console.log(multipicant,"x" ,multiplier,"=",multipicant*multiplier);
}
multipicant++;
for(let multiplier=1;multiplier<=10;multiplier++){
    console.log(multipicant+" x "+ multiplier +" = "+ multipicant*multiplier);
}
multipicant++;
for(let multiplier=1;multiplier<=10;multiplier++){
    console.log(`${multipicant} * ${multiplier} = ${multipicant*multiplier}`);
}